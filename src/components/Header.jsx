const Header = ( {info} ) => {

  return (
    <div>
        <h1>Student Task Manager</h1>
        <h2>Hello,{info}</h2>
        <p>Built to maange the tasks of the students in a better way.</p>
    </div>
  )
}

export default Header